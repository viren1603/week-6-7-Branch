const joi = require("joi");

const ImgSchemaJoi123 = joi.object().keys({
    // bio: joi.string().empty().required().min(3)
    // 'String.empty': 'email must be required',
    // }),
    // img: joi.string()
})
module.exports = ImgSchemaJoi123;
